// copyArray:
// Write a static method that takes one input, an array of double, and returns an array of double.
// The array returned should be the same length as the input, and contain exactly the same elements,
// in the same order!

public class Main {
    public static void main(String[] args){
        double[] arr = new double[3];

        arr[0] = 2.5;
        arr[1] = 24.43;
        arr[2] = 0.82;
    }
}
